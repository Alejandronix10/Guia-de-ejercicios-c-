#include <iostream>
using namespace std;

int main() {
    char caracter = 'A';  // declaramos una variable caracter y asignamos el valor 
    cout << "caracter: " << caracter << endl; // imprime lo que es el valor del caracter
    cout << "codigo ASCII: " << int(caracter) << endl; // se convierte el caracter en codigo ASCII y se imprime

    return 0;
}